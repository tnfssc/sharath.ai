# Cloudflare R2 credentials + CDN base — copy to .dev.vars and fill in.
# Values are the same ones sharath.boi.gg uses.
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ACCOUNT_ID=
R2_BUCKET_NAME=
# Overrides the CDN_BASE_URL var in wrangler.jsonc for local dev if needed.
CDN_BASE_URL=
